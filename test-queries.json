{
  "1": [
      "SELECT COUNT(*)",
      "FROM {};"
  ],
  "2": [
      "SELECT MIN(ra) AS ra_min, MAX(ra) AS ra_max, MIN(dec) AS dec_min, MAX(dec) AS dec_max",
      "FROM {};"
  ],
  "3": [
      "SELECT COUNT(*)",
      "FROM {}",
      "WHERE ra BETWEEN {} AND {}",
      "AND dec BETWEEN {} AND {};"
  ]
}
